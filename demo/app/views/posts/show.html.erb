<h1><%= @post.title %></h1>

<%# The materialized Action Text body: regular rich text, rendered the regular
    way — everything downstream of collaboration is just Action Text. %>
<%= @post.body %>

<p><%= link_to "Edit together", edit_post_path(@post) %></p>
<p><%= link_to "All posts", posts_path %></p>
