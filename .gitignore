node_modules
.DS_Store
.claude
*.tgz
package-lock.json
test/server/data
package-lock.json

# Demo-app dev artifacts. Ignored at the root because demo/.gitignore is a
# tracked file on the demo branch and vanishes from the worktree on others.
demo/log/
demo/storage/
demo/app/assets/builds/
demo/node_modules/
demo/tmp/
demo/config/master.key
demo/config/credentials.yml.enc

# Built output: `npm install` builds it (prepare); never committed.
/dist/

# The import-map e2e copies the gem's built assets here.
test/server/public/importmap-assets/
rails/pkg/

# Lexxy stylesheets copied from node_modules by build:test.
test/server/public/lexxy-css/

scripts/importmap/lexical_shim.js
