import { createBinding as e, initLocalState as t, setLocalStateFocus as s, syncCursorPositions as n, syncLexicalUpdateToYjs as i, syncYjsChangesToLexical as r } from "@lexical/yjs";
import { $createParagraphNode as o, $getRoot as c, HISTORY_MERGE_TAG as a } from "lexical";
import * as h from "yjs";
import { Doc as l, applyUpdate as d, mergeUpdates as u } from "yjs";
var p = {
	logger: "undefined" != typeof console ? console : void 0,
	WebSocket: "undefined" != typeof WebSocket ? WebSocket : void 0
}, f = { log(...e) {
	this.enabled && (e.push(Date.now()), p.logger.log("[ActionCable]", ...e));
} };
const g = () => (/* @__PURE__ */ new Date()).getTime(), m = (e) => (g() - e) / 1e3;
var b = class {
	constructor(e) {
		this.visibilityDidChange = this.visibilityDidChange.bind(this), this.connection = e, this.reconnectAttempts = 0;
	}
	start() {
		this.isRunning() || (this.startedAt = g(), delete this.stoppedAt, this.startPolling(), addEventListener("visibilitychange", this.visibilityDidChange), f.log(`ConnectionMonitor started. stale threshold = ${this.constructor.staleThreshold} s`));
	}
	stop() {
		this.isRunning() && (this.stoppedAt = g(), this.stopPolling(), removeEventListener("visibilitychange", this.visibilityDidChange), f.log("ConnectionMonitor stopped"));
	}
	isRunning() {
		return this.startedAt && !this.stoppedAt;
	}
	recordMessage() {
		this.pingedAt = g();
	}
	recordConnect() {
		this.reconnectAttempts = 0, delete this.disconnectedAt, f.log("ConnectionMonitor recorded connect");
	}
	recordDisconnect() {
		this.disconnectedAt = g(), f.log("ConnectionMonitor recorded disconnect");
	}
	startPolling() {
		this.stopPolling(), this.poll();
	}
	stopPolling() {
		clearTimeout(this.pollTimeout);
	}
	poll() {
		this.pollTimeout = setTimeout(() => {
			this.reconnectIfStale(), this.poll();
		}, this.getPollInterval());
	}
	getPollInterval() {
		const { staleThreshold: e, reconnectionBackoffRate: t } = this.constructor;
		return 1e3 * e * Math.pow(1 + t, Math.min(this.reconnectAttempts, 10)) * (1 + (0 === this.reconnectAttempts ? 1 : t) * Math.random());
	}
	reconnectIfStale() {
		this.connectionIsStale() && (f.log(`ConnectionMonitor detected stale connection. reconnectAttempts = ${this.reconnectAttempts}, time stale = ${m(this.refreshedAt)} s, stale threshold = ${this.constructor.staleThreshold} s`), this.reconnectAttempts++, this.disconnectedRecently() ? f.log(`ConnectionMonitor skipping reopening recent disconnect. time disconnected = ${m(this.disconnectedAt)} s`) : (f.log("ConnectionMonitor reopening"), this.connection.reopen()));
	}
	get refreshedAt() {
		return this.pingedAt ? this.pingedAt : this.startedAt;
	}
	connectionIsStale() {
		return m(this.refreshedAt) > this.constructor.staleThreshold;
	}
	disconnectedRecently() {
		return this.disconnectedAt && m(this.disconnectedAt) < this.constructor.staleThreshold;
	}
	visibilityDidChange() {
		"visible" === document.visibilityState && setTimeout(() => {
			!this.connectionIsStale() && this.connection.isOpen() || (f.log(`ConnectionMonitor reopening stale connection on visibilitychange. visibilityState = ${document.visibilityState}`), this.connection.reopen());
		}, 200);
	}
};
b.staleThreshold = 6, b.reconnectionBackoffRate = .15;
var y = {
	message_types: {
		welcome: "welcome",
		disconnect: "disconnect",
		ping: "ping",
		confirmation: "confirm_subscription",
		rejection: "reject_subscription"
	},
	disconnect_reasons: {
		unauthorized: "unauthorized",
		invalid_request: "invalid_request",
		server_restart: "server_restart",
		remote: "remote"
	},
	default_mount_path: "/cable",
	protocols: ["actioncable-v1-json", "actioncable-unsupported"]
};
const { message_types: v, protocols: w } = y, S = w.slice(0, w.length - 1), A = [].indexOf;
var E = class {
	constructor(e) {
		this.open = this.open.bind(this), this.consumer = e, this.subscriptions = this.consumer.subscriptions, this.monitor = new b(this), this.disconnected = !0;
	}
	send(e) {
		return !!this.isOpen() && (this.webSocket.send(JSON.stringify(e)), !0);
	}
	open() {
		if (this.isActive()) return f.log(`Attempted to open WebSocket, but existing socket is ${this.getState()}`), !1;
		{
			const e = [...w, ...this.consumer.subprotocols || []];
			return f.log(`Opening WebSocket, current state is ${this.getState()}, subprotocols: ${e}`), this.webSocket && this.uninstallEventHandlers(), this.webSocket = new p.WebSocket(this.consumer.url, e), this.installEventHandlers(), this.monitor.start(), !0;
		}
	}
	close({ allowReconnect: e } = { allowReconnect: !0 }) {
		if (e || this.monitor.stop(), this.isOpen()) return this.webSocket.close();
	}
	reopen() {
		if (f.log(`Reopening WebSocket, current state is ${this.getState()}`), !this.isActive()) return this.open();
		try {
			return this.close();
		} catch (e) {
			f.log("Failed to reopen WebSocket", e);
		} finally {
			f.log(`Reopening WebSocket in ${this.constructor.reopenDelay}ms`), setTimeout(this.open, this.constructor.reopenDelay);
		}
	}
	getProtocol() {
		if (this.webSocket) return this.webSocket.protocol;
	}
	isOpen() {
		return this.isState("open");
	}
	isActive() {
		return this.isState("open", "connecting");
	}
	triedToReconnect() {
		return this.monitor.reconnectAttempts > 0;
	}
	isProtocolSupported() {
		return A.call(S, this.getProtocol()) >= 0;
	}
	isState(...e) {
		return A.call(e, this.getState()) >= 0;
	}
	getState() {
		if (this.webSocket) {
			for (let e in p.WebSocket) if (p.WebSocket[e] === this.webSocket.readyState) return e.toLowerCase();
		}
		return null;
	}
	installEventHandlers() {
		for (let e in this.events) {
			const t = this.events[e].bind(this);
			this.webSocket[`on${e}`] = t;
		}
	}
	uninstallEventHandlers() {
		for (let e in this.events) this.webSocket[`on${e}`] = function() {};
	}
};
E.reopenDelay = 500, E.prototype.events = {
	message(e) {
		if (!this.isProtocolSupported()) return;
		const { identifier: t, message: s, reason: n, reconnect: i, type: r } = JSON.parse(e.data);
		switch (this.monitor.recordMessage(), r) {
			case v.welcome: return this.triedToReconnect() && (this.reconnectAttempted = !0), this.monitor.recordConnect(), this.subscriptions.reload();
			case v.disconnect: return f.log(`Disconnecting. Reason: ${n}`), this.close({ allowReconnect: i });
			case v.ping: return null;
			case v.confirmation: return this.subscriptions.confirmSubscription(t), this.reconnectAttempted ? (this.reconnectAttempted = !1, this.subscriptions.notify(t, "connected", { reconnected: !0 })) : this.subscriptions.notify(t, "connected", { reconnected: !1 });
			case v.rejection: return this.subscriptions.reject(t);
			default: return this.subscriptions.notify(t, "received", s);
		}
	},
	open() {
		if (f.log(`WebSocket onopen event, using '${this.getProtocol()}' subprotocol`), this.disconnected = !1, !this.isProtocolSupported()) return f.log("Protocol is unsupported. Stopping monitor and disconnecting."), this.close({ allowReconnect: !1 });
	},
	close(e) {
		if (f.log("WebSocket onclose event"), !this.disconnected) return this.disconnected = !0, this.monitor.recordDisconnect(), this.subscriptions.notifyAll("disconnected", { willAttemptReconnect: this.monitor.isRunning() });
	},
	error() {
		f.log("WebSocket onerror event");
	}
};
var k = class {
	constructor(e, t = {}, s) {
		this.consumer = e, this.identifier = JSON.stringify(t), function(e, t) {
			if (null != t) for (let s in t) e[s] = t[s];
		}(this, s);
	}
	perform(e, t = {}) {
		return t.action = e, this.send(t);
	}
	send(e) {
		return this.consumer.send({
			command: "message",
			identifier: this.identifier,
			data: JSON.stringify(e)
		});
	}
	unsubscribe() {
		return this.consumer.subscriptions.remove(this);
	}
}, C = class {
	constructor(e) {
		this.subscriptions = e, this.pendingSubscriptions = [];
	}
	guarantee(e) {
		-1 == this.pendingSubscriptions.indexOf(e) ? (f.log(`SubscriptionGuarantor guaranteeing ${e.identifier}`), this.pendingSubscriptions.push(e)) : f.log(`SubscriptionGuarantor already guaranteeing ${e.identifier}`), this.startGuaranteeing();
	}
	forget(e) {
		f.log(`SubscriptionGuarantor forgetting ${e.identifier}`), this.pendingSubscriptions = this.pendingSubscriptions.filter((t) => t !== e);
	}
	startGuaranteeing() {
		this.stopGuaranteeing(), this.retrySubscribing();
	}
	stopGuaranteeing() {
		clearTimeout(this.retryTimeout);
	}
	retrySubscribing() {
		this.retryTimeout = setTimeout(() => {
			this.subscriptions && "function" == typeof this.subscriptions.subscribe && this.pendingSubscriptions.map((e) => {
				f.log(`SubscriptionGuarantor resubscribing ${e.identifier}`), this.subscriptions.subscribe(e);
			});
		}, 500);
	}
}, I = class {
	constructor(e) {
		this.consumer = e, this.guarantor = new C(this), this.subscriptions = [];
	}
	create(e, t) {
		const s = "object" == typeof e ? e : { channel: e }, n = new k(this.consumer, s, t);
		return this.add(n);
	}
	add(e) {
		return this.subscriptions.push(e), this.consumer.ensureActiveConnection(), this.notify(e, "initialized"), this.subscribe(e), e;
	}
	remove(e) {
		return this.forget(e), this.findAll(e.identifier).length || this.sendCommand(e, "unsubscribe"), e;
	}
	reject(e) {
		return this.findAll(e).map((e) => (this.forget(e), this.notify(e, "rejected"), e));
	}
	forget(e) {
		return this.guarantor.forget(e), this.subscriptions = this.subscriptions.filter((t) => t !== e), e;
	}
	findAll(e) {
		return this.subscriptions.filter((t) => t.identifier === e);
	}
	reload() {
		return this.subscriptions.map((e) => this.subscribe(e));
	}
	notifyAll(e, ...t) {
		return this.subscriptions.map((s) => this.notify(s, e, ...t));
	}
	notify(e, t, ...s) {
		let n;
		return n = "string" == typeof e ? this.findAll(e) : [e], n.map((e) => "function" == typeof e[t] ? e[t](...s) : void 0);
	}
	subscribe(e) {
		this.sendCommand(e, "subscribe") && this.guarantor.guarantee(e);
	}
	confirmSubscription(e) {
		f.log(`Subscription confirmed ${e}`), this.findAll(e).map((e) => this.guarantor.forget(e));
	}
	sendCommand(e, t) {
		const { identifier: s } = e;
		return this.consumer.send({
			command: t,
			identifier: s
		});
	}
}, U = class {
	constructor(e) {
		this._url = e, this.subscriptions = new I(this), this.connection = new E(this), this.subprotocols = [];
	}
	get url() {
		return function(e) {
			"function" == typeof e && (e = e());
			if (e && !/^wss?:/i.test(e)) {
				const t = document.createElement("a");
				return t.href = e, t.href = t.href, t.protocol = t.protocol.replace("http", "ws"), t.href;
			}
			return e;
		}(this._url);
	}
	send(e) {
		return this.connection.send(e);
	}
	connect() {
		return this.connection.open();
	}
	disconnect() {
		return this.connection.close({ allowReconnect: !1 });
	}
	ensureActiveConnection() {
		if (!this.connection.isActive()) return this.connection.open();
	}
	addSubProtocol(e) {
		this.subprotocols = [...this.subprotocols, e];
	}
};
function T(e = function(e) {
	const t = document.head.querySelector(`meta[name='action-cable-${e}']`);
	if (t) return t.getAttribute("content");
}("url") || y.default_mount_path) {
	return new U(e);
}
const P = 1e3;
var _ = class {
	pending = [];
	#e;
	#t;
	#s;
	#n;
	#i;
	#r = 1;
	#o = !1;
	#c = void 0;
	#a = void 0;
	constructor(e) {
		const { send: t, merge: s, resendInterval: n } = e ?? {};
		if ("function" != typeof t) throw new TypeError("ReliableSync requires a send(update, id) function");
		if ("function" != typeof s) throw new TypeError("ReliableSync requires a merge(updates) function");
		this.#e = t, this.#t = s;
		const i = n ?? P;
		if (!Number.isFinite(i) || i <= 0) throw new TypeError("ReliableSync resendInterval must be a positive number");
		this.#s = i, this.#n = e.setInterval ?? ((e, t) => setInterval(e, t)), this.#i = e.clearInterval ?? ((e) => clearInterval(e));
	}
	get hasPending() {
		return this.pending.length > 0;
	}
	enqueue(e) {
		this.pending.push({
			seq: this.#r++,
			update: e
		}), this.#a = void 0, this.#o && this.#h(), this.flush();
	}
	flush() {
		this.#o && 0 !== this.pending.length && this.#e(this.#l(), this.pending[this.pending.length - 1].seq);
	}
	onAck(e) {
		!Number.isSafeInteger(e) || e < 0 || this.pending.length > 0 && e > this.pending[this.pending.length - 1].seq || (this.pending = this.pending.filter((t) => t.seq > e), this.#a = void 0, 0 === this.pending.length && this.#d());
	}
	onConnect() {
		this.#o = !0, this.flush(), this.pending.length > 0 && this.#h();
	}
	onDisconnect() {
		this.#o = !1, this.#d();
	}
	onTick() {
		this.#o && 0 !== this.pending.length && this.flush();
	}
	destroy() {
		this.#o = !1, this.#d(), this.pending = [], this.#a = void 0;
	}
	#l() {
		if (void 0 === this.#a) {
			const e = this.pending.map((e) => e.update);
			this.#a = 1 === e.length ? e[0] : this.#t(e);
		}
		return this.#a;
	}
	#h() {
		if (void 0 !== this.#c) return;
		this.#c = this.#n(() => this.onTick(), this.#s);
		const e = this.#c;
		e && "function" == typeof e.unref && e.unref();
	}
	#d() {
		void 0 !== this.#c && this.#i(this.#c), this.#c = void 0;
	}
};
const L = Math.floor, x = (Number.isNaN, Number.MAX_SAFE_INTEGER), D = (Number.MIN_SAFE_INTEGER, Number.isInteger, Number.isNaN, Number.parseInt, () => /* @__PURE__ */ new Set()), R = Array.from, N = (Array.isArray, String.fromCharCode), M = (String.fromCodePoint, N(65535), "undefined" != typeof TextEncoder ? new TextEncoder() : null), O = M ? (e) => M.encode(e) : (e) => {
	const t = unescape(encodeURIComponent(e)), s = t.length, n = new Uint8Array(s);
	for (let e = 0; e < s; e++) n[e] = t.codePointAt(e);
	return n;
};
let $ = "undefined" == typeof TextDecoder ? null : new TextDecoder("utf-8", {
	fatal: !0,
	ignoreBOM: !0
});
$ && 1 === $.decode(/* @__PURE__ */ new Uint8Array()).length && ($ = null);
var j = class {
	constructor() {
		this.cpos = 0, this.cbuf = /* @__PURE__ */ new Uint8Array(100), this.bufs = [];
	}
};
const q = () => new j(), W = (e) => {
	let t = e.cpos;
	for (let s = 0; s < e.bufs.length; s++) t += e.bufs[s].length;
	return t;
}, z = (e) => {
	const t = new Uint8Array(W(e));
	let s = 0;
	for (let n = 0; n < e.bufs.length; n++) {
		const i = e.bufs[n];
		t.set(i, s), s += i.length;
	}
	return t.set(new Uint8Array(e.cbuf.buffer, 0, e.cpos), s), t;
}, G = (e, t) => {
	const s = e.cbuf.length;
	e.cpos === s && (e.bufs.push(e.cbuf), e.cbuf = new Uint8Array(2 * s), e.cpos = 0), e.cbuf[e.cpos++] = t;
}, F = (e, t) => {
	for (; t > 127;) G(e, 128 | 127 & t), t = L(t / 128);
	G(e, 127 & t);
}, H = /* @__PURE__ */ new Uint8Array(3e4), J = H.length / 3, Y = M && M.encodeInto ? (e, t) => {
	if (t.length < J) {
		const s = M.encodeInto(t, H).written || 0;
		F(e, s);
		for (let t = 0; t < s; t++) G(e, H[t]);
	} else B(e, O(t));
} : (e, t) => {
	const s = unescape(encodeURIComponent(t)), n = s.length;
	F(e, n);
	for (let t = 0; t < n; t++) G(e, s.codePointAt(t));
}, B = (e, t) => {
	F(e, t.byteLength), ((e, t) => {
		const s = e.cbuf.length, n = e.cpos, i = (r = s - n, o = t.length, r < o ? r : o);
		var r, o;
		const c = t.length - i;
		e.cbuf.set(t.subarray(0, i), n), e.cpos += i, c > 0 && (e.bufs.push(e.cbuf), e.cbuf = new Uint8Array(((e, t) => e > t ? e : t)(2 * s, c)), e.cbuf.set(t.subarray(i)), e.cpos = c);
	})(e, t);
}, V = (e) => new Error(e), X = V("Unexpected end of array"), K = V("Integer out of Range");
var Q = class {
	constructor(e) {
		this.arr = e, this.pos = 0;
	}
};
const Z = (e) => new Q(e), ee = (e) => e.pos !== e.arr.length, te = (e) => ((e, t) => {
	const s = new Uint8Array(e.arr.buffer, e.pos + e.arr.byteOffset, t);
	return e.pos += t, s;
})(e, ne(e)), se = (e) => e.arr[e.pos++], ne = (e) => {
	let t = 0, s = 1;
	const n = e.arr.length;
	for (; e.pos < n;) {
		const n = e.arr[e.pos++];
		if (t += (127 & n) * s, s *= 128, n < 128) return t;
		if (t > x) throw K;
	}
	throw X;
}, ie = $ ? (e) => $.decode(te(e)) : (e) => {
	let t = ne(e);
	if (0 === t) return "";
	{
		let s = String.fromCodePoint(se(e));
		if (--t < 100) for (; t--;) s += String.fromCodePoint(se(e));
		else for (; t > 0;) {
			const n = t < 1e4 ? t : 1e4, i = e.arr.subarray(e.pos, e.pos + n);
			e.pos += n, s += String.fromCodePoint.apply(null, i), t -= n;
		}
		return decodeURIComponent(escape(s));
	}
}, re = (e, t, s) => ((e, t, s) => {
	F(e, 1), B(e, h.encodeStateAsUpdate(t, s));
})(t, s, te(e)), oe = (e, t, s, n) => {
	try {
		h.applyUpdate(t, te(e), s);
	} catch (e) {
		null != n && n(e), console.error("Caught error while handling a Yjs update", e);
	}
}, ce = oe, ae = (e, t, s, n, i) => {
	const r = ne(e);
	switch (r) {
		case 0:
			re(e, t, s);
			break;
		case 1:
			oe(e, s, n, i);
			break;
		case 2:
			ce(e, s, n, i);
			break;
		default: throw new Error("Unknown message type");
	}
	return r;
}, he = Date.now, le = () => /* @__PURE__ */ new Map();
var de = class {
	constructor() {
		this._observers = le();
	}
	on(e, t) {
		((e, t, s) => {
			let n = e.get(t);
			return void 0 === n && e.set(t, n = s()), n;
		})(this._observers, e, D).add(t);
	}
	once(e, t) {
		const s = (...n) => {
			this.off(e, s), t(...n);
		};
		this.on(e, s);
	}
	off(e, t) {
		const s = this._observers.get(e);
		void 0 !== s && (s.delete(t), 0 === s.size && this._observers.delete(e));
	}
	emit(e, t) {
		return R((this._observers.get(e) || le()).values()).forEach((e) => e(...t));
	}
	destroy() {
		this._observers = le();
	}
};
const ue = Symbol("Equality"), pe = Object.keys, fe = (e) => pe(e).length, ge = (e, t) => Object.prototype.hasOwnProperty.call(e, t), me = (e, t) => {
	if (e === t) return !0;
	if (null == e || null == t || e.constructor !== t.constructor && (e.constructor || Object) !== (t.constructor || Object)) return !1;
	if (null != e[ue]) return e[ue](t);
	switch (e.constructor) {
		case ArrayBuffer: e = new Uint8Array(e), t = new Uint8Array(t);
		case Uint8Array:
			if (e.byteLength !== t.byteLength) return !1;
			for (let s = 0; s < e.length; s++) if (e[s] !== t[s]) return !1;
			break;
		case Set:
			if (e.size !== t.size) return !1;
			for (const s of e) if (!t.has(s)) return !1;
			break;
		case Map:
			if (e.size !== t.size) return !1;
			for (const s of e.keys()) if (!t.has(s) || !me(e.get(s), t.get(s))) return !1;
			break;
		case void 0:
		case Object:
			if (fe(e) !== fe(t)) return !1;
			for (const s in e) if (!ge(e, s) || !me(e[s], t[s])) return !1;
			break;
		case Array:
			if (e.length !== t.length) return !1;
			for (let s = 0; s < e.length; s++) if (!me(e[s], t[s])) return !1;
			break;
		default: return !1;
	}
	return !0;
};
var be = class extends de {
	constructor(e) {
		super(), this.doc = e, this.clientID = e.clientID, this.states = /* @__PURE__ */ new Map(), this.meta = /* @__PURE__ */ new Map(), this._checkInterval = setInterval(() => {
			const e = he();
			null !== this.getLocalState() && 15e3 <= e - this.meta.get(this.clientID).lastUpdated && this.setLocalState(this.getLocalState());
			const t = [];
			this.meta.forEach((s, n) => {
				n !== this.clientID && 3e4 <= e - s.lastUpdated && this.states.has(n) && t.push(n);
			}), t.length > 0 && ye(this, t, "timeout");
		}, L(3e3)), e.on("destroy", () => {
			this.destroy();
		}), this.setLocalState({});
	}
	destroy() {
		this.emit("destroy", [this]), this.setLocalState(null), super.destroy(), clearInterval(this._checkInterval);
	}
	getLocalState() {
		return this.states.get(this.clientID) || null;
	}
	setLocalState(e) {
		const t = this.clientID, s = this.meta.get(t), n = void 0 === s ? 0 : s.clock + 1, i = this.states.get(t);
		null === e ? this.states.delete(t) : this.states.set(t, e), this.meta.set(t, {
			clock: n,
			lastUpdated: he()
		});
		const r = [], o = [], c = [], a = [];
		null === e ? a.push(t) : null == i ? null != e && r.push(t) : (o.push(t), me(i, e) || c.push(t)), (r.length > 0 || c.length > 0 || a.length > 0) && this.emit("change", [{
			added: r,
			updated: c,
			removed: a
		}, "local"]), this.emit("update", [{
			added: r,
			updated: o,
			removed: a
		}, "local"]);
	}
	setLocalStateField(e, t) {
		const s = this.getLocalState();
		null !== s && this.setLocalState({
			...s,
			[e]: t
		});
	}
	getStates() {
		return this.states;
	}
};
const ye = (e, t, s) => {
	const n = [];
	for (let s = 0; s < t.length; s++) {
		const i = t[s];
		if (e.states.has(i)) {
			if (e.states.delete(i), i === e.clientID) {
				const t = e.meta.get(i);
				e.meta.set(i, {
					clock: t.clock + 1,
					lastUpdated: he()
				});
			}
			n.push(i);
		}
	}
	n.length > 0 && (e.emit("change", [{
		added: [],
		updated: [],
		removed: n
	}, s]), e.emit("update", [{
		added: [],
		updated: [],
		removed: n
	}, s]));
}, ve = 0, we = 1;
var Se = class {
	doc;
	awareness;
	#e;
	#u;
	#p = !1;
	#f;
	#g;
	#m;
	constructor(e, t) {
		const { send: s, awareness: n = null, resendInterval: i, onError: r, setInterval: o, clearInterval: c } = t ?? {};
		if (!e) throw new TypeError("YProtocolSession requires a Y.Doc");
		if ("function" != typeof s) throw new TypeError("YProtocolSession requires a send(frame, id) function");
		this.doc = e, this.awareness = n, this.#e = s, this.#u = r ?? ((e, t) => console.warn(`[yrby] ${t}:`, e)), this.#f = new _({
			merge: u,
			send: (e, t) => this.#e(this.#b(e), t),
			resendInterval: i,
			setInterval: o,
			clearInterval: c
		}), this.#g = (e, t) => {
			t !== this && this.#f.enqueue(e);
		}, this.doc.on("update", this.#g), this.awareness && (this.#m = ({ added: e, updated: t, removed: s }, n) => {
			if (n === this) return;
			const i = e.concat(t, s);
			this.#e(this.#y(i), void 0);
		}, this.awareness.on("update", this.#m));
	}
	get synced() {
		return this.#p;
	}
	get hasPending() {
		return this.#f.hasPending;
	}
	onConnect() {
		this.#e(this.#v(), void 0), this.awareness && null !== this.awareness.getLocalState() && this.#e(this.#y([this.doc.clientID]), void 0), this.#f.onConnect();
	}
	onDisconnect() {
		if (this.#p = !1, this.#f.onDisconnect(), this.awareness) {
			const e = [...this.awareness.getStates().keys()].filter((e) => e !== this.doc.clientID);
			e.length && ye(this.awareness, e, this);
		}
	}
	removeLocalAwareness() {
		this.awareness && null !== this.awareness.getLocalState() && this.awareness.setLocalState(null);
	}
	ack(e) {
		this.#f.onAck(e);
	}
	applyRemoteUpdate(e) {
		d(this.doc, e, this);
	}
	receive(e) {
		try {
			if (null === this.#w(e)) return null;
			const t = Z(e), s = q();
			switch (ne(t)) {
				case ve: {
					F(s, ve);
					const e = ae(t, s, this.doc, this);
					this.#p || 1 !== e || (this.#p = !0);
					break;
				}
				case we:
					this.awareness && ((e, t, s) => {
						const n = Z(t), i = he(), r = [], o = [], c = [], a = [], h = ne(n);
						for (let t = 0; t < h; t++) {
							const t = ne(n);
							let s = ne(n);
							const h = JSON.parse(ie(n)), l = e.meta.get(t), d = e.states.get(t), u = void 0 === l ? 0 : l.clock;
							(u < s || u === s && null === h && e.states.has(t)) && (null === h ? t === e.clientID && null != e.getLocalState() ? s++ : e.states.delete(t) : e.states.set(t, h), e.meta.set(t, {
								clock: s,
								lastUpdated: i
							}), void 0 === l && null !== h ? r.push(t) : void 0 !== l && null === h ? a.push(t) : null !== h && (me(h, d) || c.push(t), o.push(t)));
						}
						(r.length > 0 || c.length > 0 || a.length > 0) && e.emit("change", [{
							added: r,
							updated: c,
							removed: a
						}, s]), (r.length > 0 || o.length > 0 || a.length > 0) && e.emit("update", [{
							added: r,
							updated: o,
							removed: a
						}, s]);
					})(this.awareness, te(t), this);
					break;
				default: return null;
			}
			return W(s) > 1 ? z(s) : null;
		} catch (e) {
			return this.#u(e, "receive"), null;
		}
	}
	destroy() {
		this.doc.off("update", this.#g), this.awareness && this.#m && this.awareness.off("update", this.#m), this.#f.destroy();
	}
	#v() {
		const e = q();
		return F(e, ve), ((e, t) => {
			F(e, 0);
			const s = h.encodeStateVector(t);
			B(e, s);
		})(e, this.doc), z(e);
	}
	#b(e) {
		const t = q();
		return F(t, ve), ((e, t) => {
			F(e, 2), B(e, t);
		})(t, e), z(t);
	}
	#y(e) {
		const t = q();
		return F(t, we), B(t, ((e, t, s = e.states) => {
			const n = t.length, i = q();
			F(i, n);
			for (let r = 0; r < n; r++) {
				const n = t[r], o = s.get(n) || null, c = e.meta.get(n).clock;
				F(i, n), F(i, c), Y(i, JSON.stringify(o));
			}
			return z(i);
		})(this.awareness, e)), z(t);
	}
	#w(e) {
		const t = Z(e), s = ne(t);
		switch (s) {
			case ve: {
				const e = new l();
				try {
					const s = q();
					F(s, ve), ae(t, s, e, this);
				} finally {
					e.destroy();
				}
				break;
			}
			case we:
				{
					const e = te(t), s = Z(e), n = ne(s);
					for (let e = 0; e < n; e++) ne(s), ne(s), JSON.parse(ie(s));
					if (ee(s)) throw new Error("awareness payload has trailing bytes");
				}
				break;
			default: return null;
		}
		if (ee(t)) throw new Error("frame has trailing bytes after a complete message");
		return s;
	}
};
var Ae = class {
	doc;
	consumer;
	channelName;
	channelParams;
	awareness;
	session;
	#S = null;
	#u;
	#o = !1;
	#A = "disconnected";
	#E = /* @__PURE__ */ new Set();
	#k = null;
	#C = !1;
	#I = null;
	#U = null;
	#T = null;
	constructor(e, t, s, n = {}, i = {}) {
		this.doc = e, this.consumer = t, this.channelName = s, this.channelParams = n, this.awareness = new be(e), this.#u = i.onError ?? ((e, t) => console.warn(`[yrby] ${t}:`, e)), this.session = new Se(e, {
			awareness: this.awareness,
			resendInterval: i.resendInterval,
			onError: this.#u,
			send: (e, t) => this.#e(e, t)
		});
	}
	get synced() {
		return this.session.synced;
	}
	get whenSynced() {
		return this.#k ??= this.#C ? Promise.resolve() : new Promise((e) => {
			const t = this.onStatusChange(({ status: s }) => {
				"synced" === s && (t(), e());
			});
		}), this.#k;
	}
	get hasPending() {
		return this.session.hasPending;
	}
	applyRemoteUpdate(e) {
		this.session.applyRemoteUpdate(e);
	}
	get status() {
		return this.#A;
	}
	onStatusChange(e) {
		return this.#E.add(e), () => this.#E.delete(e);
	}
	connect() {
		if (this.#S) return;
		const e = this;
		this.#S = this.consumer.subscriptions.create({
			channel: this.channelName,
			...this.channelParams
		}, {
			received(t) {
				if (t && void 0 !== t.ack) return void e.session.ack(t.ack);
				const s = t && t.awareness, n = t && (s ?? t.update);
				if ("string" != typeof n) return;
				let i;
				try {
					r = n, i = Uint8Array.from(atob(r), (e) => e.charCodeAt(0));
				} catch (t) {
					e.#u(t, "received");
					return;
				}
				var r;
				if (void 0 !== s && i[0] !== we) return void e.#u(/* @__PURE__ */ new Error("awareness envelope carried a non-awareness frame"), "received");
				const o = e.session.receive(i);
				o && e.#e(o, void 0), e.#P();
			},
			connected() {
				e.#o = !0, e.session.onConnect(), e.#P();
			},
			disconnected() {
				e.#o = !1, e.session.onDisconnect(), e.#P();
			},
			rejected() {
				e.#u(/* @__PURE__ */ new Error("subscription rejected by the server"), "rejected"), e.disconnect();
			}
		}), this.#_(), this.#P();
	}
	disconnect() {
		if (!this.#S) return;
		const e = this.#S;
		this.session.removeLocalAwareness(), this.session.onDisconnect(), this.#o = !1, this.#S = null, this.#L(), queueMicrotask(() => e.unsubscribe?.()), this.#P();
	}
	destroy() {
		this.disconnect(), this.session.destroy(), this.awareness.destroy(), this.#E.clear();
	}
	#x() {
		return this.#S ? this.#o ? this.session.synced ? "synced" : "connected" : "connecting" : "disconnected";
	}
	#P() {
		const e = this.#x();
		if (e !== this.#A) {
			this.#A = e, "synced" === e && (this.#C = !0);
			for (const t of this.#E) t({ status: e });
		}
	}
	#_() {
		"undefined" == typeof window || this.#I || (this.#I = () => {
			this.#T = this.awareness.getLocalState(), this.session.removeLocalAwareness();
		}, this.#U = (e) => {
			e.persisted && this.#T && (null === this.awareness.getLocalState() && this.awareness.setLocalState(this.#T), this.#T = null);
		}, window.addEventListener("pagehide", this.#I), window.addEventListener("pageshow", this.#U));
	}
	#L() {
		"undefined" != typeof window && (this.#I && (window.removeEventListener("pagehide", this.#I), this.#I = null), this.#U && (window.removeEventListener("pageshow", this.#U), this.#U = null));
	}
	#e(e, t) {
		const s = this.#S;
		if (!s) return;
		const n = (i = e, btoa(Array.from(i, (e) => String.fromCharCode(e)).join("")));
		var i;
		const r = e[0] === we;
		try {
			if (r && "function" == typeof s.whisper) return void this.#D(s.whisper({ awareness: n }));
			const e = void 0 === t ? { update: n } : {
				update: n,
				id: t
			};
			this.#D(s.send(e));
		} catch (e) {
			this.#u(e, "send");
		}
	}
	#D(e) {
		e instanceof Promise && e.catch((e) => this.#u(e, "send"));
	}
};
let Ee;
var ke = class extends HTMLElement {
	#R = null;
	connectedCallback() {
		this.editorElement = this.closest("lexxy-editor"), this.editorElement ? (this.editor = this.editorElement.editor, this.editor ? this.#N() : this.editorElement.addEventListener("lexxy:initialize", () => {
			this.editor = this.editorElement.editor, this.#N();
		}, { once: !0 })) : console.error("<lexxy-collaboration> must be placed inside a <lexxy-editor>.");
	}
	disconnectedCallback() {
		this.#R?.();
	}
	#N() {
		const h = this.getAttribute("doc-id") || "main", d = this.getAttribute("name") || "Example User", u = this.getAttribute("color") || "#958DF1", p = this.getAttribute("channel-name") || "SyncChannel", f = this.getAttribute("channel-params") || "{}";
		let g;
		try {
			g = "string" == typeof f ? JSON.parse(f) : f;
		} catch {
			console.error("<lexxy-collaboration>: invalid channel-params attribute (expected JSON); using {}.", f), g = {};
		}
		const m = !this.provider, b = !this.doc, y = this.doc || new l(), v = this.provider || new Ae(y, this.consumer || (Ee ??= T()), p, g);
		m && v.connect();
		const w = v.awareness, S = /* @__PURE__ */ new Map();
		S.set(h, y);
		const A = this.editor.getEditorState();
		this.editor.update(() => c().clear(), {
			tag: a,
			discrete: !0
		});
		const E = function(e) {
			const t = /* @__PURE__ */ new Map(), s = e?._nodes;
			return s && "function" == typeof s.forEach ? (s.forEach((e, s) => {
				Ie.has(s) && t.set(e.klass, Ce);
			}), t) : t;
		}(this.editor), k = e(this.editor, v, h, y, S, E);
		(function(e) {
			const t = e?.root?.constructor?.prototype;
			if (!t || "function" != typeof t.splice || t.__yrbySplicePatched) return;
			const s = t.splice;
			t.splice = function(e, t, n, i) {
				if (void 0 !== this._children[t] || void 0 !== i) return s.call(this, e, t, n, i);
			}, t.__yrbySplicePatched = !0;
		})(k);
		const C = function(e, t, s) {
			const n = e.registerUpdateListener(({ dirtyElements: n, dirtyLeaves: r, editorState: o, normalizedNodes: c, prevEditorState: a, tags: h }) => {
				e.getEditorState().read(() => {
					!1 === h.has("skip-collab") && i(s, t, a, o, n, r, c, h);
				});
			}), o = (e, n) => {
				n.origin !== s && r(s, t, e, !1);
			};
			return s.root.getSharedType().observeDeep(o), () => {
				n(), s.root.getSharedType().unobserveDeep(o);
			};
		}(this.editor, v, k), I = function(e, t, s, n) {
			let i = !1;
			const r = () => {
				if (!i && t.synced && (i = !0, clearInterval(h), 0 === s.root.getSharedType().length)) {
					if (n && !n.read(() => {
						const e = c();
						if (0 === e.getChildrenSize()) return !0;
						const t = 1 === e.getChildrenSize() && e.getFirstChild();
						return !!t && "paragraph" === t.getType() && 0 === t.getChildrenSize();
					})) return void e.setEditorState(n, { tag: a });
					e.update(() => {
						const e = c();
						e.clear(), e.append(o());
					}, { tag: a });
				}
			}, h = setInterval(r, 50);
			"function" == typeof h?.unref && h.unref();
			return () => {
				i = !0, clearInterval(h);
			};
		}(this.editor, v, k, A), U = this.#M();
		k.cursorsContainer = U, t(v, d, u, !0, {
			name: d,
			color: u
		}), s(v, d, u, !0, {
			name: d,
			color: u
		});
		const P = () => n(k, v);
		w.on("update", P);
		const _ = this.editor.registerUpdateListener(P);
		n(k, v), this.provider = v, this.doc = y, this.awareness = w, this.binding = k, this.#R = () => {
			this.#R = null, w.off("update", P), _(), C(), I(), U.remove(), m && (v.disconnect(), this.provider = null), b && (this.doc = null);
		};
	}
	#M() {
		const e = this.editorElement.querySelector(".lexxy-editor-container") || this.editorElement;
		"static" === getComputedStyle(e).position && (e.style.position = "relative");
		const t = document.createElement("div");
		return t.className = "lexxy-collab-cursors", t.style.cssText = "position:absolute;inset:0;pointer-events:none;", e.appendChild(t), t;
	}
};
const Ce = /* @__PURE__ */ new Set([
	"editor",
	"file",
	"previewSrc",
	"uploadUrl",
	"blobUrlTemplate"
]), Ie = /* @__PURE__ */ new Set([
	"action_text_attachment",
	"action_text_attachment_upload",
	"custom_action_text_attachment"
]);
customElements.get("lexxy-collaboration") || customElements.define("lexxy-collaboration", ke);
export { ke as Collaboration, Ae as YrbyProvider };
